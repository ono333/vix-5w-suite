# VIX Suite — Session Handoff (Sat Jul 18, 2026)

Paste this into a new chat to continue. Context: Shin runs an automated UVXY
short-call income system (V1–V5 variants) on an Ubuntu ZBOX
(`shin@192.168.100.142`, `~/vix_suite/`), Tradier sandbox paper trading,
systemd timer fires Mon/Fri 10:15 ET. Philosophy: income is OUTPUT never
INPUT; hold risk constant; decline when edge absent. Broker is truth.

## Live paper book (Tradier sandbox, as of Fri Jul 17 close)
- Short Jul 24 calls: $26.5C (V2, cr 0.66), $27C (V5 Mon, cr 0.34),
  $28.5C (V3 first-ever entry, cr 0.27), $29C (V5 Fri, cr 0.27). Total cr $154.
- Longs: Aug 21 $23C, Dec 18 $36C.
- Jul 17 legs ($26C x2, $37C, $34C x3 long) expired OTM (UVXY closed 25.44,
  VIX 18.77) — full wins; sandbox sweeps them by Monday.
- Fri Jul 17 run = first GUARDED-tier live test: 77-min run, 66 reprice rows,
  wide-spread flags 68–74%, all 3 fills landed. DB `quotes` now 221 rows.

## What was built/changed today (all committed, repo = git in ~/vix_suite)
1. `shadow_strategist.py` — forward-only shadow book, runs via systemd
   ExecStartPost after each orchestrator run. Three strategies at mid fills:
   vertical (short ~7DTE delta≤0.30 + wing +$3), diagonal (same short +
   ~35DTE long, rolls short), long_spike (BTO ~100DTE ~0.30d call when VIX
   pctile <25, STC at 2.5x / pctile≥75 / DTE<21). FIRST long_spike entry
   bootstrapped to skip the calm gate (one-time test; treat its P&L as
   plumbing verification only). Tables: shadow_positions/trades/marks,
   vix_history (seeded 9,231 CBOE closes — CLOSE column, corrected).
   `python3 shadow_strategist.py --report` = per-strategy P&L.
   `log_live_takebid()` hook exists but NOT yet wired into orchestrator.
2. `risk_logger.py` — daily risk dials into `risk_measures` table (3,773 days
   seeded). Cron 16:50 ET weekdays. Sources: yfinance (VIX/VVIX/SPX) + CBOE
   CSVs (VIX3M/VIX9D — Yahoo lags them). Measures: slope_ratio=VIX/VIX3M,
   ratio_9d=VIX9D/VIX, vvix_ratio, vrp=VIX−rv21, spread_flag_rate (own
   quotes-table wide-spread share). `--report` shows percentiles.
   Fri close read: slope .914 (66pct, contango intact), 9d/30d .898 (32pct,
   spike priced as fading), VRP 7.1 (84pct, FAT premium), spread rate .576.
   => "rich premium, decaying event" — favors holding shorts. Contrast:
   UBS Turbu-lens at 0.9 says months-scale fragility. Both now logged daily;
   event-study later (P(VIX+50% in 21d | signal) vs base rate).
3. Repo hygiene: git init baseline `e5719af` (recover old files:
   `git show e5719af:<file>`), deleted 130+ .bak/patch/fix one-shots,
   reorg: leaves moved to archive/ tools/ analysis/, docs/dependency_map.txt
   has full import graph + entry-point inventory. Root now ~28 files.
   12 entry points pinned to root by systemd + cron — DO NOT MOVE:
   orchestrator, long_manager+executor, alert_engine, daily_signal,
   intraday_alert, mon/fri member_signal, thursday_member_alert, app.py
   (8501), tradier_monitor.py (8502), shadow_strategist. Cron: vol_triangle
   capture_snapshot 16:45 (writes snapshot load_snap() reads at 10:15!),
   DB backup 18:00 -> ~/backups/, risk_logger 16:50.
4. Fixed: half-dollar strike display bug (`:.0f` -> `:g`, 13 sites, commit
   f9e6920). UVXY weeklys have half-dollar strikes; execution always handled
   them, only display truncated.
5. Removed second venv `vix_env/` question — resolved: only setup_automation
   (archived) referenced it. Live venv = `venv/` (python3.10).

## Orchestrator behavior facts (reviewed today)
- Long roll: DTE≤threshold (V1 30/V2 21/V3 14/V4 21/V5 21) -> BTO new then
  STC old. Known gap: if STC leg doesn't fill, state still updates, old long
  orphaned silently (broker sync should catch; add alert someday).
- Short defense: NONE automated. Delta≥0.50 -> log + email alert only.
  Delta≥0.35 -> advisory alert. Roll path never executed live yet.
- Shorts: DTE 1–2 let expire; DTE 0 enter new; else hold.

## Monday Jul 20, 10:15 ET — what to expect / check
- Live: V2 $26.5C is ~$1 OTM (delta .39 Fri) — watch delta log; alert-only
  if ≥0.50. V1/V4 may evaluate fresh entries. Jul 17 legs should be swept.
- Shadow: first vertical + diagonal entries; bootstrap long_spike BTO.
- Check with: `tail -40 ~/vix_suite/orchestrator.log`,
  `python3 shadow_strategist.py --report`, `python3 risk_logger.py --report`,
  timer: `systemctl list-timers vix_tradier_orch.timer --no-pager`.

## Open items (deliberately deferred)
- Wire `log_live_takebid()` one-liner into orchestrator fill confirmation.
- Regime consolidation: regime_detector (11 importers) AND vol_triangle
  (4 importers, incl. live path) are BOTH active — merge someday, quiet week.
- Archive newly-dead leaves: backup_ui, maybe gdrive_backup.
- Unfilled-STC alert in long roll; log the emergency-check exception.
- STC slippage sign cosmetic bug in tradier_exec_log.py (data correct).
- Event-study risk_measures vs own fills/P&L after ~1 month of data.
- Second 8501 app.py systemd unit duplicate — check/disable stale one.
- Fidelity live side: weekly_card.py first real roll was planned Fri Jul 17
  — status unknown, ask Shin.

## Working preferences (honor these)
- No-hallucination code: Python/Pine/Streamlit. Concise. Plain numbered
  lists for orders. Shell commands as bare lines. Verify state via
  systemctl/broker files, never memory. Surgical patches, never wholesale
  rewrites. py_compile before deploy. For .py delivery:
  `scp ~/Downloads/<file>.py shin@192.168.100.142:~/vix_suite/<file>.py`
  then test + `git add -A && git commit`.
- Sandbox fills understate live (~-$0.08); never calibrate on them.
- Two timers on one account = position-doubling risk. $0-bid = skip.
  Backwardation = hard stand-down.
